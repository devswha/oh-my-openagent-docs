---
title: Oracle
description: Read-only architecture/debugging consultant. Stub for ja.
---

GPT-native read-only consultant. Sustained reasoning across unfamiliar patterns and multi-system tradeoffs.

**Default model:** `gpt-5.5` (high). Fallback: GPT → Gemini-3.1-pro (high) → Opus (max) → GLM-5.1.

**Restrictions:** blocked from `write`, `edit`, `task`, `call_omo_agent`. Never override to MiniMax.

## Source Notes

Aligned with upstream `docs/reference/features.md` and `src/shared/model-requirements.ts`.
