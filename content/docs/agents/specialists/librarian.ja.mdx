---
title: Librarian
description: Multi-repo analysis and doc lookup. Stub for ja.
---

Retrieval lane. Evidence-based answers from codebase + library docs. Speed-first, intentionally cheap.

**Default:** `gpt-5.4-mini-fast`. Fallback: → Qwen3.5-plus → MiniMax-m2.7-highspeed → MiniMax-m2.7 → Claude Haiku → GPT-5.4-nano.

**Restrictions:** read-only. Blocked from `write`, `edit`, `task`, `call_omo_agent`. Never override to Opus.

## Source Notes

Aligned with upstream `docs/reference/features.md` and `src/shared/model-requirements.ts`.
