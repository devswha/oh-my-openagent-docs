---
title: Interactive Bash
description: Tmux-based terminal for TUI apps. Stub for zh.
---

`interactive_bash(tmux_command="...")` — pass tmux subcommands directly (no `tmux` prefix). Use for vim, htop, pudb, anything needing a persistent terminal across turns. One-shot commands stay on `Bash`.

## Source Notes

Aligned with upstream `docs/reference/features.md`.
