---
title: Delegation
description: task, call_omo_agent, background_output, background_cancel. Stub for ja.
---

Four tools — `task` (category-based), `call_omo_agent` (explore/librarian), `background_output`, `background_cancel`. `run_in_background: true` keeps the orchestrator moving.

Blocked for read-only agents (oracle, librarian, explore, multimodal-looker, momus, atlas).

## Source Notes

Aligned with upstream `docs/reference/features.md`.
