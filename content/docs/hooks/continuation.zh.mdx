---
title: Continuation
description: Hooks that refuse to let agents stop while work is unfinished. Stub for zh.
---

`todo-continuation-enforcer`, `compaction-todo-preserver`, `ralph-loop`, `start-work`, `auto-slash-command`, `stop-continuation-guard`, `agent-usage-reminder`.

Continuation is load-bearing for `ultrawork` and ralph loops. Disable specific hooks via `disabled_hooks` rather than turning off the tier.

## Source Notes

Aligned with upstream `docs/reference/features.md`.
